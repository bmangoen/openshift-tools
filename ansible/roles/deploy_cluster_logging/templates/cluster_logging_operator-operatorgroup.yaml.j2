---
apiVersion: operators.coreos.com/v1
kind: OperatorGroup
metadata:
  name: "{{ cluster_logging_operator_operatorgroup.name }}"
  namespace: "{{ cluster_logging_operator_namespace.name }}"
spec:
  targetNamespaces:
{% for namespace in cluster_logging_operator_operatorgroup.spec.targetNamespaces %}
  - "{{ namespace }}"
{% endfor %}
